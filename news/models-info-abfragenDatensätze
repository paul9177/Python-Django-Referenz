abfragen Datensätze - select, bei neustart shell import nicht vergessen
>>>m        # zeigt alles zur klasse (object) an, da ja alles in variable m ist
>>>m.id     # id anzeigen

abfragen Datensätze - select (seite 936 u.f.)
>>>Meldung.objects.all()

// out - <QuerySet [<Meldung: Unsere erste Meldung>, <Meldung: Umfrage zu Django>, <Meldung: Ein neuer Eintrag nach shell <h1>Start</h1>>]>
// auch nach shell neustart

>>>Kommentar.objects.filter(meldung__id=1)

