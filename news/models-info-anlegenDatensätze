anlegen Datensätze - insert, wird shell neu gestartet m aus dem speicher aber daten in DB
# anlegen datensätze per shell

1.1.Meldung 1 aus models.py class Meldung START - import nicht vergessen
    >>>from news.models import Meldung, Kommentar
    >>>from datetime import datetime

    >>>m = Meldung(titel='Unsere erste Meldung',
                   zeitstempel=datetime.today(),
                   text="Klassischer Weise steht hier 'Hello Welt'.")
    >>>m.save()

1.2.Meldung 2
    >>>from news.models import Meldung, Kommentar
    >>>from datetime import datetime

    >>>m2 = Meldung(titel='Umfrage zu Django',
                   zeitstempel=datetime.today(),
                   text="Wie findet ihr das Framework")
    >>>m2.save()

1.3.Meldung 2
    >>>from news.models import Meldung, Kommentar
    >>>from datetime import datetime

    >>>m2 = Meldung(titel='Kommentar erstellen Fehler, was ist es',
                   zeitstempel=datetime.today(),
                   text="Ein Kommentar in der Shell erstellen ohne Bezug zu einer Meldung wirft einen Fehler, da m nicht vorhanden ist")
    >>>m2.save()

2.1.Kommentar 1 aus models.py class Kommentar START - import nicht vergessen
    >>>from news.models import Meldung, Kommentar
    >>>from datetime import datetime

    >>>k1 = m2.kommentar_set.create(autor='Daniel', text='der Kommentar sollte vor dem speichern von m erstellt werden.') # falsch erst m.save()
    >>>k1 = m2.kommentar_set.create(autor='Daniel', text='vorher Kommentar falsch erst m speichern und dann Kommentar erstellen.')





m.save() // warum m.save() und nicht k.save(), weil kommentar mit meldung verknüpft ist